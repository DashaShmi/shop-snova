{
  "name": "backend",
  "version": "1.0.0",
  "description": "бекенд для SNOVA",
  "main": "index.js",
  "scripts": {
    "start": "ts-node src/test.ts",
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "tsc && cd dist && zip -r yandex_dist.zip ./*"
  },
  "author": "Dasha Shmizha",
  "license": "ISC",
  "devDependencies": {
    "ts-node": "^10.9.2",
    "typescript": "^5.9.3"
  }
}